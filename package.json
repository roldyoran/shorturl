{
  "name": "shorturl",
  "private": true,
  "workspaces": ["backend", "frontend"],
  "scripts": {
    "dev:front": " bun run --cwd frontend dev",
    "dev:back": " bun run --cwd backend dev",
    "build:front": " bun run --cwd frontend build"
  },
  "devDependencies": {
    "shadcn-vue": "^2.4.3"
  },
  "dependencies": {
    "canvas-confetti": "^1.9.4"
  }
}
